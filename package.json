{
  "scripts": {
    "ship": "node ship.js",
    "predeploy": "cd client && npm install && npm run build",
    "deploy": "gh-pages -d client/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
